# Datasets
This folder contains preprocessing scripts for our included datasets, as well as import scripts which are moved to the cloud instances to import data into the datastore. A basic visualisation of the datasets is also included here. 
